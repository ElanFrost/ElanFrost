#include <stdio.h>

int main() {
    int a, b, c;
    printf("Find whole numbers in interval [a, b], that are divisible by c with a remainder of 1\nEnter whole numbers a, b and c:\n");

    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);

    return 0;
}
